#include <bits/stdc++.h>
using namespace std;

struct nad{
	int nodo, lat;
	nad *next;
};

struct nli{
	bool v;
	nad *list;
};

void addadj(nli *no, int ve, int la){
	nad *x = (nad*)
	
}

int main(){
	int N, n, m, s, t, w, s1, s2;
	scanf("%d",&N);
	for(int i=0;i<N;i++){
		scanf("%d%d%d%d",&n,&m,&s,&t);
		nli servers[n];	/*={NULL};*/for(int k=0;k<n;k++){servers[k].v=false;servers[k].list=NULL;}
		for(int j=0; j<m; j++){
			scanf("%d%d%d",&s1,&s2,w);

		}
	}
	return 0;
}
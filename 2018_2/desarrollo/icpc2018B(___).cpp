#include <bits/stdc++.h>
using namespace std;

int main(){
	int n, t=0;
	scanf("%d",&n);
	int trees[n];
	for (int i = 0; i < n; ++i){
		scanf("%d",&trees[i]);
		t+=trees[i];
	}
	t/=2;
	for(int i = 0; i < n; ++i){
		/* code */
	}
	//cout << t << endl;
	return 0;
}
